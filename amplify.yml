version: 1
backend:
  phases:
    build:
      commands:
        - npm install
        - amplifyPush --simple
  cache:
    paths:
      - node_modules/**/*
applications:
  - frontend:
      phases:
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'